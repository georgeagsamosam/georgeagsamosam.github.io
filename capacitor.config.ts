import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'georgeagsamosam.github.io',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
